export const metadata = {
  title: "Submission Guidelines | ADSoC 6.0",
  description: "Detailed instructions for paper formatting, submission process, and registration guidelines for ADSoC 6.0.",
};

export default function GuidelinesLayout({ children }) {
  return children;
}
